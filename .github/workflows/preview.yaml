name: Test Logs

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Step 1 executed"

      - name: Step 2
        run: echo "Step 2 executed"
